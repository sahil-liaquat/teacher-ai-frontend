import { Suspense } from "react";
import { ResourcesWorkspace } from "@/components/school-admin/resources/resources-workspace";

export default function SchoolResourcesPage() {
  return <Suspense><ResourcesWorkspace /></Suspense>;
}
