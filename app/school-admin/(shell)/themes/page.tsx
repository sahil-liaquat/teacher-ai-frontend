import { Suspense } from "react";
import { ThemesWorkspace } from "@/components/school-admin/themes/themes-workspace";

export default function SchoolThemesPage() {
  return <Suspense><ThemesWorkspace /></Suspense>;
}
