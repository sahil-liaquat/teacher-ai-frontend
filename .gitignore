# v0 runtime files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules
.next/
tsconfig.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store
.env.example
.env

# TeachPad Primary printables (~900MB) — served from Cloudinary, never committed.
# Restore locally from ../primary-assets-staging/ if you need the library to render.
/public/primary-resources/

# Claude Code worktrees (created via EnterWorktree during SDD execution)
.claude/
